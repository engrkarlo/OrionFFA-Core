OrionFFACore arena schematics

Use //wand or //pos1 + //pos2 to select an arena, then run:
/ffa arena save <arena-id>

The plugin saves <arena-id>.schem here and records the selection minimum point as the default paste target.
Timed resets are controlled by arena-reset.schedule and arena-reset.arenas.<id>.scheduled in config.yml.
